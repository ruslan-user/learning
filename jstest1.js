//JS Test #1

	var count = 4;
	console.log(count);


	var users = ['Bob', 'Mike', 'Joey', 'Max', 'Sam'];



	var logUsers =  function logUsers() {
		for (i = [0]; i < 5; i++) {
			var i = users[i];
			console.log(users[]);
		}
	}



	var logUserByIndex = function logUserByIndex(index) {
		var index = users[];
		console.log(index);

	}

	var logLastUser = function logLastUser(){

	}
	
	
	var actions = {
		logUsers = logUsers(),
		logUserByIndex = logUserByIndex(),
		logLastUser = logLastUser()
	}